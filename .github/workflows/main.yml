name: CI
'on':
  push:
    branches:
      - github*
      - master
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch: null
concurrency:
  group: '${{ github.ref }}'
  cancel-in-progress: true
jobs:
  test:
    timeout-minutes: 30
    name: test1
    runs-on: ubuntu-latest
    container:
      image: 'ysoftwareab/yp-ubuntu-20.04-minimal:latest'
      options: '--user yp:yp'
    steps:
      - shell: bash
        run: id -u; id -g
      - shell: bash
        run: echo "HOME=/home/yp" | sudo tee -a $GITHUB_ENV
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
      - shell: bash
        run: 'touch ~/test1; sudo touch /github/home/test1; sudo touch ${GITHUB_WORKSPACE}/test1'
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
      - uses: ysoftwareab/yplatform@master
        with:
          run: |-
            id -u; id -g
            ls -la ${HOME}
            touch ~/test1-action; sudo touch /github/home/test1-action; sudo touch ${GITHUB_WORKSPACE}/test1-action
            ls -la ${HOME}
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
  test2:
    timeout-minutes: 30
    name: test2
    runs-on: ubuntu-latest
    container:
      image: 'ysoftwareab/yp-ubuntu-20.04-minimal:latest'
      options: '--user yp:yp'
    steps:
      - shell: bash
        run: id -u; id -g
      - shell: bash
        run: echo "HOME=/home/yp" | sudo tee -a $GITHUB_ENV
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
      - shell: bash
        run: 'touch ~/test2; sudo touch /github/home/test2; sudo touch ${GITHUB_WORKSPACE}/test2'
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
      - uses: ysoftwareab/yplatform@master
        with:
          run: |-
            id -u; id -g
            ls -la ${HOME}
            touch ~/test2-action; sudo touch /github/home/test2-action; sudo touch ${GITHUB_WORKSPACE}/test2-action
            ls -la ${HOME}
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
  test3:
    timeout-minutes: 30
    name: test3
    runs-on: ubuntu-latest
    container:
      image: 'ysoftwareab/yp-ubuntu-20.04-minimal:latest'
      options: '--user yp:yp'
    steps:
      - shell: bash
        run: id -u; id -g
      - shell: bash
        run: echo "HOME=/home/yp" | sudo tee -a $GITHUB_ENV
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
      - shell: bash
        run: 'touch ~/test3; sudo touch /github/home/test3; sudo touch ${GITHUB_WORKSPACE}/test3'
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
      - uses: ysoftwareab/yplatform@master
        with:
          run: |-
            id -u; id -g
            ls -la ${HOME}
            touch ~/test3-action; sudo touch /github/home/test3-action; sudo touch ${GITHUB_WORKSPACE}/test3-action
            ls -la ${HOME}
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/home; ls -la $GITHUB_WORKSPACE'
    needs:
      - test1
      - test2
