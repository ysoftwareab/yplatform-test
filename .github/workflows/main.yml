name: CI
'on':
  push:
    branches:
      - github*
      - master
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch: null
concurrency:
  group: '${{ github.ref }}'
  cancel-in-progress: true
jobs:
  test1:
    timeout-minutes: 30
    name: test1
    runs-on: ubuntu-latest
    container:
      image: 'ysoftwareab/yp-ubuntu-20.04-minimal:latest'
      options: '--user yp:yp'
    steps:
      - shell: bash
        run: id -u; id -g
      - shell: bash
        run: echo "HOME=/home/yp" | sudo tee -a $GITHUB_ENV
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
      - shell: bash
        run: 'touch ~/test1; sudo touch /github/{home,workflow,file_commands,workspace}/test1'
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
      - uses: ysoftwareab/yplatform@master
        with:
          run: |-
            id -u; id -g
            ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true
            touch ~/test1-action; sudo touch /github/{home,workflow,file_commands,workspace}/test1-action
            ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
  test2:
    timeout-minutes: 30
    name: test2
    runs-on: ubuntu-latest
    container:
      image: 'ysoftwareab/yp-ubuntu-20.04-minimal:latest'
      options: '--user yp:yp'
    steps:
      - shell: bash
        run: id -u; id -g
      - shell: bash
        run: echo "HOME=/home/yp" | sudo tee -a $GITHUB_ENV
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
      - shell: bash
        run: 'touch ~/test2; sudo touch /github/{home,workflow,file_commands,workspace}/test2'
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
      - uses: ysoftwareab/yplatform@master
        with:
          run: |-
            id -u; id -g
            ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true
            touch ~/test2-action; sudo touch /github/{home,workflow,file_commands,workspace}/test2-action
            ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
  test3:
    timeout-minutes: 30
    name: test3
    runs-on: ubuntu-latest
    container:
      image: 'ysoftwareab/yp-ubuntu-20.04-minimal:latest'
      options: '--user yp:yp'
    steps:
      - shell: bash
        run: id -u; id -g
      - shell: bash
        run: echo "HOME=/home/yp" | sudo tee -a $GITHUB_ENV
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
      - shell: bash
        run: 'touch ~/test3; sudo touch /github/{home,workflow,file_commands,workspace}/test3'
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
      - uses: ysoftwareab/yplatform@master
        with:
          run: |-
            id -u; id -g
            ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true
            touch ~/test3-action; sudo touch /github/{home,workflow,file_commands,workspace}/test3-action
            ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true
      - shell: bash
        run: 'ls -la ${HOME}; ls -la /github/{home,workflow,file_commands,workspace} || true'
    needs:
      - test1
      - test2
